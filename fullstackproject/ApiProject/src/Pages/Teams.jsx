import { useContext, useState } from "react";
import toast from "react-hot-toast";
import { useMutation } from "@tanstack/react-query";
import { getTeams } from "../api/ApiRoutes/Routes";
export default function Teams() {
  return (
    <div>Teams</div>
  )
}
